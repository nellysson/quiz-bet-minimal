# Permitir scripts de build para sharp e core-js
public-hoist-pattern[]=sharp
public-hoist-pattern[]=core-js
# Ou, para permitir todos os scripts de build (menos seguro, mas resolve o problema)
# unsafe-perm=true

# Ignorar avisos sobre scripts de build
ignore-scripts=false
